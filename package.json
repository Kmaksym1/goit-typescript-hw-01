{
  "name": "goit-typescript-hw-01",
  "version": "1.0.0",
  "description": "- встановлюємо глобально `npm install -g typescript` - створили файл та зробили його коміляцію `tsc test.ts` - стежимо за зміною файла в режимі реального часу `tsc test.ts -w` - створюємо файл tsconfig.json `tsc --init`",
  "main": "test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-dev-server --node-resolve --open --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.3.6"
  }
}
